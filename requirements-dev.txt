# ============================================================
# Development / test dependencies
# Source of truth: pyproject.toml [project.optional-dependencies.dev]
# Keep in sync.
# ============================================================
-r requirements.txt

pillow>=10
pytest>=8
pytest-cov>=5
pytest-mock>=3.14
requests-mock>=1.12
# LLM (Change 007): only needed for live LLM tests; not required for
# the default dev install. Mock-friendly in tests/unit/.
litellm>=1.40 ; python_version >= "3.10"
