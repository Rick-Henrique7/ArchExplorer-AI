"""{{ component.name }} — CLI tool auto-generated by ArchExplorer LPS."""
from __future__ import annotations

import argparse
import sys


def main(argv: list[str] | None = None) -> int:
    """Entry point — parses args, dispatches to the right subcommand."""
    parser = argparse.ArgumentParser(
        prog="{{ component.name | lower | replace(' ', '-') }}",
        description="{{ component.description or '' }}",
    )
    parser.add_argument(
        "--version",
        action="version",
        version="{{ component.name }} 0.1.0",
    )
    sub = parser.add_subparsers(dest="command", required=True)

    # Default subcommand — replace with real ones via metadata if needed.
    hello = sub.add_parser("hello", help="Print a greeting")
    hello.add_argument("name", nargs="?", default="world")

    args = parser.parse_args(argv)

    if args.command == "hello":
        print(f"Hello, {args.name}!")
        return 0

    parser.print_help()
    return 2


if __name__ == "__main__":
    sys.exit(main())
